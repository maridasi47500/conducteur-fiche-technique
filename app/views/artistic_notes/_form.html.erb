<%= form_with(model: artistic_note) do |form| %>
  <% if artistic_note.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artistic_note.errors.count, "error") %> prohibited this artistic_note from being saved:</h2>

      <ul>
        <% artistic_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
