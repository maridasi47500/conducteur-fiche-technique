<%= form_with(model: emotional_marker) do |form| %>
  <% if emotional_marker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(emotional_marker.errors.count, "error") %> prohibited this emotional_marker from being saved:</h2>

      <ul>
        <% emotional_marker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
