<%= form_with(model: stage_theme) do |form| %>
  <% if stage_theme.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stage_theme.errors.count, "error") %> prohibited this stage_theme from being saved:</h2>

      <ul>
        <% stage_theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
